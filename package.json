{
  "name": "zenith-productivity-hub",
  "version": "1.0.0",
  "description": "_Balance your work and life, not just your tasks._",
  "homepage": "https://github.com/DeathHunterX/zenith-productivity-hub#readme",
  "bugs": {
    "url": "https://github.com/DeathHunterX/zenith-productivity-hub/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/DeathHunterX/zenith-productivity-hub.git"
  },
  "scripts": {
    "setup:client": "node scripts/setup.js client",
    "setup:server": "node scripts/setup.js server",
    "setup:docs": "node scripts/setup.js docs",
    "setup:all": "npm run setup:client && npm run setup:server && npm run setup:docs",
    "dev:client": "node scripts/run.js client dev",
    "dev:server": "node scripts/run.js server dev",
    "dev:docs": "node scripts/run.js docs dev",
    "dev:all": "npm run dev:client & npm run dev:server",
    "build:client": "node scripts/run.js client build",
    "build:server": "node scripts/run.js server build",
    "build:docs": "node scripts/run.js docs build",
    "build:all": "npm run build:client && npm run build:server && npm run build:docs",
    "start:client": "node scripts/run.js client start",
    "start:server": "node scripts/run.js server start",
    "start:docs": "node scripts/run.js docs serve"
  },
  "license": "ISC",
  "author": "",
  "main": "index.js",
  "type": "module"
}
